# 记忆存档：《砸个痛快》发泄小游戏 完整制作过程

> 存档时间：2026-08-25。本文件是给"新开窗口继续开发"用的交接文档，读完即可无缝续上 M2。

## 一、项目概况

| 项目 | 内容 |
|---|---|
| 游戏名 | 《砸个痛快》（备选：《发泄屋》《摔个稀碎》） |
| 定位 | 减压发泄小游戏：砸碎房间里的一切，听清脆碎裂声，过关得积分，兑换礼品升级。**不用复杂，够发泄、有爽感** |
| 形态 | 纯 HTML 单文件网页游戏（零依赖零构建），手机触屏竖屏 + 电脑鼠标 |
| **永久链接** | **https://kikifan-hub.github.io/-/** |
| GitHub 仓库 | https://github.com/kikifan-hub/- （公开，main 分支，Pages 已开启） |
| 本地目录 | /Volumes/AI_WORK/AI_OUTPUTS/项目总开关/游戏/发泄小游戏/ |
| Obsidian | 库：`/Users/fanwenji/Documents/Kiki-obsidian/Kiki-obsidian/游戏/砸个痛快/` |

## 二、当前进度（截至 2026-08-26）

- ✅ 设计方案 v1.0（[设计方案.md](设计方案.md)：玩法/10 关/经济系统/音效配方/里程碑）
- ✅ **M1 手感原型**（[index.html](index.html)，第 1 关「客厅」9 件物品，全部爽感机制已实现）
- ✅ **M2 完整核心**：3 关（客厅/厨房/办公室）+ 选关界面 + localStorage 存档（总积分/各关星级/最高连击/14 级称号），冒烟测试 18 项全绿（详见"六、M2 实现清单"）
- ✅ **M3 系统**：礼品馆（12 件礼品积分门槛兑换）+ 荣誉室陈列 + 称号界面（详见"七、M3 实现清单"），冒烟测试 34 项全绿
- ✅ 已上线 GitHub Pages（M3 commit b3d51b3，线上验证 200）
- ⏳ **下一步：M4 扩 10 关 + 自由发泄屋**（关卡纯数据，好加）：浴室（大镜子）、瓷器店、实验室、老板办公室、宴会厅（香槟塔）、古董拍卖会（限时）、终极发泄屋；第 3 关后解锁自由发泄屋（无尽刷分）

## 三、M1 已实现清单（index.html 内的实际代码）

- 拖拽甩出（抛物线预览）+ 单击轻砸；Pointer Events 统一触屏/鼠标
- 4 材质碎裂音效（玻璃/陶瓷/木头/电子，WebAudio 合成）、投掷嗖声、重物砸地"砰"、碎片落地弹跳"叮"、裂纹"咔"、连击琶音、通关号角
- 顿帧（40~120ms 按质量）、震屏（质量×2px 上限 14）、子弹时间（连锁≥3 → 0.35x/1.2s；大件 → 0.5x/0.5s）、暗角
- 连击 ×1.2~×5（3s 窗口）、连锁（碎片多米诺，+5 分 +50%）、裂纹阶段（耐久≥2 物品）
- 通关：砸碎 ≥60%；1/2/3 星（60/80/100%）；3 星 ×1.5 积分；结算面板（继续砸场 / 再砸一次）
- 手机震动 navigator.vibrate、碎片上限 150 对象池式回收、粉尘粒子、积分飘字

## 四、技术架构速查（接手必读）

```
index.html 内模块顺序：
配置(W=480,H=854,GROUND=720,GRAV=2200,MULT倍率) → 工具(rnd/clamp/shade/polyPath/roundRect)
→ 画布初始化(cv/ctx/DPR) → 音频引擎 AU → 物品类型 TYPES → 关卡数据 LEVELS/TABLES
→ makeItems() → 存档模块 Save（key 'zmt_save_v1'）→ 称号 TITLES/titleInfo() → 游戏状态 G
→ 输入 input → 破坏(smash/takeDamage/makeFragments) → 物理 step()
→ 渲染 render()/drawRoom/drawDeco/drawItem/drawHUD/drawPanel/drawSelect
→ 主循环 frame()（仅 screen==='play' 时步进物理）
```

关键约定：
- 物品 = 多边形轮廓 poly + 细节绘制 + 材质属性（hp 耐久/score 分值/mass 质量/thresh 碎裂阈值/颜色），见 TYPES
- 关卡 = 纯数据（LEVELS[i].items: {t:类型,x,s:位置(table1/table2/floor/wall)} + 主题配色 theme + 装饰 deco 描述数组），**加关只加数据**
- 界面切换 = G.screen（'select' | 'play'）+ startLevel(n)/showLevelSelect()，面板 3 按钮：再砸一次 / 下一关（末关为再砸一次）/ 选关
- 存档 = Save.save()/load()/unlocked(n)，schema {score, stars[], best[], maxCombo}；每关结算用差分入账（banked/award 差）防止重复计分；"继续砸场"后需 100% 全清才再次结算
- 爽感参数全部集中在 smash()/takeDamage()/step() 里，改手感直接找这三处
- 物品状态机：rest（静止可拾取）→ held（手持）→ fly（飞行/被撞翻）→ gone（已碎）
- 连锁判定：碎片/被撞翻物品撞碎其他物品 → source='chain' → 连锁加分 + 慢动作触发
- 拾取支持物时上方物品自动塌落（支撑移除）：pickup 循环里 jt.y<it.y-4 且水平重叠的 rest 物品转 fly
- 桌面 TABLES 与地面同撞击规则（落桌面可摔碎/弹起/静置）

## 五、已踩过的坑（重要！别再踩）

1. **`cv` 初始化顺序**：canvas 初始化必须在 addEventListener 之前（const TDZ 报错，已修复，勿再挪动）
2. **本机网络有代理**：cloudflared 隧道被挡（QUIC/7844 不通）；localtunnel 可用但慢（8~20s 加载）。现在 Pages 已上线，无需隧道
3. **`.claude/settings.json` 曾被误提交**：已在提交 546c960 移除，.gitignore 已含 `.claude/`
4. **GitHub Pages 开启**：需在网页端 Settings→Pages→Deploy from a branch→main→/(root)→Save；保存后按钮变灰、"Learn how to add a Jekyll theme"出现 = 已开启。API 开启需令牌带 Pages 权限
5. **推送认证**：本机无 GitHub 凭据，推送需 PAT（Contents 读写）。每次推送临时用 `git push https://x-access-token:令牌@github.com/kikifan-hub/-.git main`，**令牌不写入任何文件，用完即弃**
6. **Pages 构建延迟**：push 后 1~2 分钟生效；验证用 `curl https://kikifan-hub.github.io/-/index.html` 看 200 + 34313 bytes
7. 测试工具：Playwright（Python 版）可用，冒烟测试脚本在 /tmp/zm_smoke.py（改游戏后用它回归）
8. **微信内置浏览器锁音频**：iOS 微信 WKWebView 在 WeixinJSBridge 就绪前锁死 WebAudio、且反复挂起 AudioContext——Safari 有声音 ≠ 微信有声音。解法（commit 11edcd6）：监听 WeixinJSBridgeReady 解锁（已就绪则 invoke 触发）+ unlock() 每次从挂起恢复都重新预热 + touchend/click 兜底；安卓微信（XWeb 内核）一般无此问题，已实测安卓微信出声
9. **地面撞击伤害是死代码（M1 遗留，M2 修复）**：canvas y 轴向下为正，坠落时 vy>0，原代码 `const imp=-it.vy` 恒为负 → `imp>150` 永不成立 → 摔落永远静默落地（M1 的"松手摔碎"其实只靠砸中物品/撞墙触发）。修复：`imp=Math.abs(it.vy)`、弹起方向 `vy=-Math.abs(it.vy)*0.35`（地面与桌面两处都要改）。改后摔落/单击轻砸/盘子塔塌落都会按撞击强度摔碎

## 六、M2 实现清单（2026-08-25 完成）

1. **3 个关卡**（LEVELS 数组，每关主题配色 theme + 装饰 deco 描述）：
   - 第 1 关 客厅（暖色，窗+挂画，9 件）——M1 内容升级
   - 第 2 关 厨房（蓝白，置物架+挂钟，12 件）：桌上 5 层盘子塔（同列 5 盘叠放），一砸整塔多米诺；新物品碗/水壶（陶瓷 2 耐久）
   - 第 3 关 办公室（灰蓝，白板+挂钟+窗，10 件）：显示器（电子 2 耐久 100 分）/文件堆（纸）+ 咖啡杯/相框
2. **选关界面**：3 张关卡卡片（主题色块+迷你房间预览+星级），锁定关卡灰色带锁，通关解锁下一关
3. **localStorage 存档**：key `zmt_save_v1`，{score 总积分, stars[] 各关星级, best[] 各关最佳分, maxCombo 最高连击}；刷新持久化
4. **等级称号**：14 级阈值表 TITLES（100 见习摔手 → 30000 发泄之神），选关页脚 + 结算面板显示当前称号
5. **每关结算**：1/2/3 星（60/80/100%），3 星 ×1.5；总积分差分入账防重复；面板 3 按钮（再砸一次/下一关/选关），"继续砸场"后需全清才再结算

架构落地：LEVEL → LEVELS 数组；G.screen 'select'/'play' 界面切换；桌面 TABLES 与地面同撞击规则；拾取支持物上方物品自动塌落。冒烟测试 /tmp/zm_smoke.py 18 项全绿（含显示器两耐久、盘子塔多米诺、刷新存档持久化、称号计算）。

## 七、M3 实现清单（2026-08-26 完成）

1. **礼品兑换馆（shop 界面）**：12 件虚拟礼品（小黄鸭 200 → 发泄之神像 150000），全部程序绘制（g.draw 函数，每件 1 个 canvas 绘制块）；**兑换规则 = 累计积分达到门槛即可领取、不扣分**（与设计方案 6.4 数值节奏一致："10 关全通 ≈ 1.2~1.5 万分 → 能领前 6~7 件"）；兑换赠称号徽章 + 上行音阶铃铛音效（AU.redeem）+ toast 提示 + 手机震动；未达标卡片变暗显示"还差 X 分"，已领取显示绿勾
2. **荣誉室（hall 界面）**：4×3 陈列台（底座+颈），已兑换礼品上台陈列，空展位虚线问号+价格；下方各关最佳成绩榜（星级+最佳分）；底部称号徽章墙（礼品称号芯片）
3. **称号界面（titles 界面）**：当前称号大卡 + 进度条；14 级称号两列列表（达成金色/未达成灰色，显示等级号按设计方案 1,2,3…10,12,15,20,25）；礼品称号徽章区
4. **选关页底部导航**：3 个按钮（🎁 礼品馆 / 🏆 荣誉室 / 🏅 称号），G.screen 扩展 'shop'/'hall'/'titles'
5. **存档扩展**：schema 加 `gifts:[]`（已兑换 id 数组），load 兼容旧存档（无 gifts 字段 → []），key 不变 `zmt_save_v1`
6. **TITLES 重构**：每级加显示等级号（[100,'见习摔手',1]…[30000,'发泄之神',25]），titleInfo 返回 {lv,ti,cur,next}

架构落地：菜单系界面共用 drawBG/drawHeader（返回按钮 G.backBtn）/drawToast 三件套；pointerdown 按 G.screen 分支；渲染分发在 render()。冒烟测试 /tmp/zm_smoke.py 重建为 34 项（M2 回归 22 项 + M3 新测 12 项：门槛不足防兑换/兑换不扣分/重复兑换防重/刷新持久化/全量 12 件/徽章计数/全程零 JS 异常），另加像素级视觉采样验证礼品绘制。

## 八、M4 待办（按设计方案，接手就做）

1. **扩到 10 关**：浴室（大镜子，玻璃 1 击+连锁反射）、瓷器店（高分目标）、实验室（试管烧瓶小型群）、老板办公室（电视 3 耐久/奖杯/金属保险箱当工具砸不碎）、宴会厅（香槟杯塔）、古董拍卖会（限时 60 秒）、终极发泄屋（全素材混合）——关卡纯数据，加关只加 LEVELS 项 + 新 TYPES
2. **自由发泄屋**：第 3 关后解锁，无尽物品、无目标、纯刷分解压
3. 新物品类型：镜子、试管、香槟杯、奖杯、保险箱（金属不可碎可当工具）、鱼缸（碎裂带水花）
4. 新音效：玻璃"叮当"群、金属撞击声、限时倒计时音

## 九、工作原则（延续执行）

1. **爽感优先**：音效/震屏/顿帧/慢动作是第一优先级，任何改动先保证"砸得爽"
2. **保持简单**：一关加一两件新东西就够，玩法不加复杂度
3. **数据驱动**：关卡 JSON 定义，加关不改代码
4. **手机优先**：触屏手感优先，鼠标兼容即可
5. 每次改动后用无头浏览器冒烟测试（/tmp/zm_smoke.py 可改造复用）
